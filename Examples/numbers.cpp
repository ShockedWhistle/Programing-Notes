#include <iostream>
#include <cmath>

using namespace std;

int main()
{

    int num = 1;
    num++; // It adds one Same rules

    cout << pow(2, 5);

    /*
    c++ follows order of operations
    Modulus = % so 10 % 3 = 1 as there is 1 remainder

    10/3 = 3
    10.0 / 3 = 3.3333

    integer with integer always = integer no matter what.

    pow( 2 , 3) = 2*2*2
    sqrt(36) = 6
    round(4.3) = 4
    ceil(4.1) = 5
    floor(4.8) = 4
    fmax( 3 , 10 ) = 10 returns larger number
    fmin( 3 , 10 ) = 3 returns smaller number
    */

    return 0;

}